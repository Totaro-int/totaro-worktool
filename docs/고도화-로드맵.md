# totaro-worktool 고도화 로드맵

> MVP 작동 중. 약점 3가지: ① 챗 추출이 휴리스틱이라 노이즈 ② 요약이 태준 Mac에 묶임(SPOF)
> ③ 판매하려면 멀티테넌트·보안·웹캡처가 없음. 아래를 **순서대로 하나씩 만들고 검증**하며 진행.

## Tier 1 — 신뢰성

- [ ] **#1 중앙 요약기 SPOF 제거** — `summarize-chat-raw` 를 Supabase Edge Function / 서버로 이전.
      _게이트: Anthropic API 키(과금) 필요._ (현재는 태준 Mac launchd 의존)
- [ ] **#2 챗 추출 정식화** — V8 문자열 휴리스틱 → Chromium IndexedDB 정식 파서로 대화 단위 추출.
- [x] **#4 운영 가시성** — `scripts/status.mjs`: 멤버별 마지막 로그·chat_raw 대기·네이버 동기화 상태.
- [ ] **#3 chat_raw 보존정책** — 요약 끝난 원문 N일 후 purge(민감정보·용량).

## Tier 2 — 제품 완성도

- [ ] **#5 통합 활동 피드 + 주간 자동 보고서** — 깃허브·네이버·챗·할일 한 곳 + 사장님 주간 요약.
- [ ] **#6 로그 검색·추세** — claude_logs 검색 + 주간 작업량/영역별 추세.
- [ ] **#7 멤버 정합성** — 승주 정식 로그인 → `업플로우` 임시매핑 정리.

## Tier 3 — 판매(SaaS)

- [ ] **#8 멀티테넌트** — org 분리, 설정 외부화, 온보딩 플로우.
- [ ] **#9 보안** — 공유 anon → 개인 토큰, 업로드 전 PII 스크럽, 암호화.
- [ ] **#10 브라우저 확장** — claude/gpt/gemini 웹챗 응답 캡처 (데스크탑 IndexedDB 의존 탈피, 멀티벤더).
- [ ] **#11 결제·플랜·사용량 한도.**

## 별개 (열림)

- [ ] 라이브 배포 URL 점검 (자동배포 끊겼는지)
- [ ] 네이버 커머스 워커 첫 실행 (화이트리스트 머신)
- [ ] 옛 `import-claude-desktop-chats.mjs` 정리 (raw-uploader+summarizer 가 대체)

## 진행 원칙

1. 한 번에 하나씩 — 만들고 **검증(실측)** 후 다음.
2. 게이트(API 키·배포·승주 로그인) 있는 항목은 표시하고 사용자 확정 받기.
3. 추천 순서: **#1 서버 요약기 → #2 정식 파싱 → #10 확장**. (#1+#10 이 "팔 수 있는 제품"의 핵심)

## 진행 로그

**자율 세션 (게이트 없는 코드만):**

- **#4 운영 가시성** — `scripts/status.mjs` (`npm run status`). ✅
- **#2 추출 품질** — 대화파일 단위 게이트 + 모지바케·위기상담 노이즈 필터 강화. (휴리스틱 개선;
  완전 정밀은 IndexedDB 정식 파서 = 향후. 의미 필터는 중앙 요약기가 최종 담당) 🟡 부분
- **#9 PII 스크럽** — 업로드 전 이메일·전화·토큰 마스킹. 🟡 부분(스크럽만, 인증 토큰화·암호화는 향후)
- **#3 보존정책** — 요약완료 원문 30일 후 purge (요약기에 포함). ✅
- **데브 통일** — `setup-logging.sh` 가 챗을 원문 업로더로 (비개발자와 동일 경로). 옛
  `import-claude-desktop-chats.mjs` 제거. ✅
- **#1 서버 요약기 코드** — `supabase/functions/summarize-chat-raw/` Edge Function 작성.
  _배포 보류: Anthropic API 키(과금) + `supabase functions deploy` 필요._ 🟡 코드만

**다음(게이트 풀리면):** #1 배포(API 키) · #2 정식 파서 · #5/#6 앱기능(배포 검증) · #10 확장.
